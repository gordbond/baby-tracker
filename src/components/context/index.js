import React from 'react';


const babyTrackerContext = React.createContext();
export const Provider = babyTrackerContext.Provider;
export const Consumer = babyTrackerContext.Consumer;

export default Consumer;